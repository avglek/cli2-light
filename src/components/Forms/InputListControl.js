import React from 'react'
import { Typography } from '@material-ui/core'

const InputListControl = ({ data }) => {
  return <Typography variant="h6">Input List</Typography>
}

export default InputListControl
