import React from 'react'
import { Typography } from '@material-ui/core'

const SelectControl = ({ data }) => {
  console.log('D:', data)
  return <Typography variant='h6'>select</Typography>
}

export default SelectControl
